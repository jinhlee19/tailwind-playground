const ButtonSq = ({text}) => {
  return (
    <div>
        <button className='inline-block bg-black px-8 py-[11px] text-white  rounded-md text-xs tracking-widest uppercase'>{text}</button>
    </div>
  )
}

export default ButtonSq